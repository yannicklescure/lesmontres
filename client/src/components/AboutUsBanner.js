import styled from "styled-components";

const AboutUsBanner = () => {
  return <MainWrapper>about us banner</MainWrapper>;
};

const MainWrapper = styled.div`
  background-color: white;
`;

export default AboutUsBanner;
